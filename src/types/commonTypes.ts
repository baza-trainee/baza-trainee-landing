export type TSlide = {
  // title: string;
  // specialization?: string;
  // text: string;
  // image: string;
  __v: number;
  _id: string;
  imageUrl: string;
  subtitle: { en: string; pl: string; ua: string };
  title: { en: string; pl: string; ua: string };
};
